package dev.com.item.compartison.domain.enums

enum class ErrorTypeEnum {
    DOMAIN_ERROR,
    FILE_PARSE_ERROR,
    GATEWAY_ERROR,
    ILLEGAL_ARGUMENT_ERROR,
    GENERIC_ERROR,
    NOT_FOUND_ERROR,
}
